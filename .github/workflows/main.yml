  name: frist_workflow
  
  on:
    push: 
      branches:
        - main
  jobs:
    exanple_job:
      runs-on: ubuntu-latest


      steps:
        - name: print a welcome message
          run: echo "welcome "

        - name: checkout repo 
          uses: actions/checkout@v4.2.2


        - name: list repo files 
          run: ls -la 
